fastapi>=0.115
uvicorn[standard]>=0.32
sqlmodel>=0.0.22
httpx[socks]>=0.27
pydantic>=2.8
python-multipart>=0.0.9
sse-starlette>=2.1
tenacity>=9.0
typer>=0.12
# Reading generation metadata straight out of image files (Ingest → folder).
# numpy carries the LSB/alpha-channel bit walk, which is far too slow per-pixel
# in pure Python.
pillow>=10.0
numpy>=1.26
# Stage 3 tag classification. The default provider is OpenAI and the SDK
# also drives any OpenAI-compatible endpoint (OpenRouter, Groq, ...), so
# installing it here keeps the documented default working from a fresh
# clone. `anthropic` is only needed if you select that provider.
openai>=1.50
anthropic>=0.34
